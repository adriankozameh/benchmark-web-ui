# Benchmark Mobile

Reserved for the Expo / React Native application.

When mobile work starts, create the Expo app here and reuse:

- `@benchmark/domain`
- `@benchmark/api`
- `@benchmark/validation`

The mobile app should implement its own Cognito redirect/session adapter using Expo-compatible secure storage and deep links rather than reusing browser `sessionStorage`.
